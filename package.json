{
  "name": "ital-group-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ital Group — general construction. WebGL one-pager.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify.mjs",
    "check": "vite build && node scripts/verify.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
